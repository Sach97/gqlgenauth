scalar DateTime

type Query {
  me: User
}

type Mutation {
  signup(email: String!, password: String!): Instructions!
  login(email: String!, password: String!): AuthPayload!
}

type AuthPayload {
  token: String!
  user: User!
}

type Instructions {
  text: String!
}

type User {
  id: ID!
  email: String!
  name: String!
  createdAt: DateTime!
}